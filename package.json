{
  "name": "gate-runner",
  "version": "1.0.0",
  "description": "Gate Runner - jeu web 3D low-poly (army gate runner)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
}
